import * as React from "react";
import stylesCategoria from "./Categoria.module.scss";
import type { ICategoriaProps } from "./ICategoriaProps";
// import { SPHttpClient, SPHttpClientResponse } from "@microsoft/sp-http";

const Categoria: React.FC<ICategoriaProps> = (props) => {
//   const { currentSiteUrl, spHttpClient, categoria } = props;
  const { } = props;

  return(<div className={stylesCategoria.categoria}>

  </div>);
};

export default Categoria;
